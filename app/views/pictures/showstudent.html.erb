<% if student_signed_in? %>
  <%= render "home/header" %>
  <% end %>
  <% if admin_signed_in?%>
  <%= render "home/header1"%>
  <% end%>
  <%= @simg.pictureable_type%>
  <%# <%= @simg.url%> %>
<%= image_tag @simg.url %>
<%= link_to "Back", :back, class:"btn btn-primary"%>