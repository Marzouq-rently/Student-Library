<%= render "home/header"%>
<table class="table">
  <thead>
    <tr>
     <th scope="col">id</th>
       <th scope="col">student_id</th>
       <th scope="col">student email</th>
    <th scope="col">bookid</th>
    <th scope="col">Book name</th>
    <th scope="col">Author</th>
    <th scope="col">Status</th>
   
          </tr>
    <div id="books">
    <% if @stbohis!=nil%>
   <td> <%= @stbohis.id%></td>
    <td><%= @stbohis.student_id%></td>
    <% stud=Student.find(@stbohis.student_id)%>
<th scope="row"><%= stud.email %></th>
<td><%= @stbohis.book_id %></td>
<% bok=Book.find(@stbohis.book_id)%>
<td><%= bok.bookname %></td>
<td><%= bok.author %></td>
<td>Borrowed</td>
<% end %>
<% if @strehis!=nil%>
   <td> <%= @strehis.id%></td>
    <td><%= @strehis.student_id%></td>
    <% stud=Student.find(@strehis.student_id)%>
<th scope="row"><%= stud.email %></th>
<td><%= @strehis.book_id %></td>
<% bok=Book.find(@strehis.book_id)%>
<td><%= bok.bookname %></td>
<td><%= bok.author %></td>
<td>Reserved</td>
<% end %>
</div>

  </thead>
  
  </table>
  <%= link_to "BACK", :back, class:"btn btn-secondary"%>